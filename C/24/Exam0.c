// &는 주소를 기리킴

// 문자열 배열: 끝을 나타내는 특별한 표시 "널(null)" 문자공간 필요
// char str1[4] = {'a', 'b', 'c'};  // 배열을 만들 때 마지막에 null문자가 들어감
// 내가 원하는 글자수+1 만큼 배열의 크기를 정의해야 함

// str[0] = 'a';
// str[1] = 'b';
// str[2] = 'c';
// str[3] = '\0'; // 문자열의 끝을 나타내는 null 문자

// 다차원배열
// int array[2][3] => [2]는 행, [3]은 열
// array[0][0]  array[0][1]  array[0][2]  // 첫 번째 행
// 
